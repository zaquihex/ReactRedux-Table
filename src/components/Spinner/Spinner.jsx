import React from 'react';

import './Spinner.css';
//Simple spinner to indicate the user that the application is waiting for the answer of some call
const spinner = () => (
    <div className="Loader">Loading...</div>
);

export default spinner;